input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-5.1.36.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://mysql-db:3306/employees"
    jdbc_user => "root"
    jdbc_password => "changeme"
    parameters => { "gender_filter" => "F" }
    schedule => "* * * 2 *"
    statement => "SELECT * from employees where gender = :gender_filter"
  }
}

filter {
}
output {
    stdout { codec => rubydebug }
}